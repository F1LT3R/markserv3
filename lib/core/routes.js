// const Routtes = require('app/lib/http/routes')

// module.exports = (config) => {
// 	const Init = {}

// 	Init.config = {
// 		MarkconfDir: process.cwd()
// 	}

// 	const server = Server(Init.config)
// 	const requests = Requests(Init.config)
// 	const routes = Requests(Init.config)

// 	const serverConfig = {
// 		protocol: 'http',
// 		port: 8001,
// 		host: 'localhost',
// 		handler: requests.handler
// 	}

// 	Init.httpServer = server.create(serverConfig)

// 	return Init
// }
